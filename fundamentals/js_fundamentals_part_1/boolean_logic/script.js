// And, Or, Not Operators for Booleans

const age = 16;

const a = age >= 20;
const b = age < 30;

console.log('value of a ' + a); // false
console.log('Not a ' + !a); // true

// Not operator has precendence over And, Or operator